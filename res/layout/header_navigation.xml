<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark" >

    <ImageView
        android:id="@+id/header_navigation_imageview_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingRight="8dp"
        android:src="@mipmap/ic_launcher"
        app:srcCompat="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/header_navigation_textview_author"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/header_navigation_imageview_icon"
        android:layout_alignLeft="@+id/header_navigation_textview_version"
        android:text="@string/boxplay_navigation_author" />

    <TextView
        android:id="@+id/header_navigation_textview_version"
        android:layout_width="287dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/header_navigation_textview_author"
        android:layout_toRightOf="@+id/header_navigation_imageview_icon"
        android:paddingBottom="3dp"
        android:text="@string/boxplay_navigation_version"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

</RelativeLayout>